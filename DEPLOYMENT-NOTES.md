# Deployment notes

This upload includes the prebuilt `dist/` output because the provided Astro source snapshot is incomplete on Linux (`src/pages/` and `functions/` are missing from the folder that was provided).

For Cloudflare Pages, deploy the `dist/` directory directly from this repo snapshot.
